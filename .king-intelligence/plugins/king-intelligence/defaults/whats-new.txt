Fixed for good: the memory tidy and the hours sync now run from the newest toolkit on your computer, and the copies inside your folder refresh themselves every time Claude Code opens (they used to fall behind the toolkit without saying so). Your memory index now tidies itself before it hits the limit, even if you never run end-session, and nothing is ever deleted. One hours number from now on: the measured one on your members page; the old estimate file only counts which tools ran. New: /king-intelligence:health-check, a one-shot check and repair you can run any time something looks off. Also works when Claude Code's settings folder was moved to a custom location.

Fixed: three quiet failures at session close, found by a member on Windows. Your hours now get recorded even when the measuring step crashes on its way out (it printed the right numbers, then the close called it a failure anyway). The memory tidy now reads the bullet-style memory index Claude Code writes by default, so an index that had grown past what loads gets trimmed, and it ages out your oldest notes, never your newest. Folder notes are stamped with your machine's real time zone instead of Eastern. Nothing to do: this update is the fix.
