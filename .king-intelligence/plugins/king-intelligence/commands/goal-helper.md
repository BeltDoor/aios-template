---
description: "Design a loop worth running before you fire a /goal: sharpen the goal, turn 'done' into real checks, set a second set of eyes, and bake in guardrails so the run can't burn time. Type it before any 'keep working until it's done' job."
argument-hint: "[what you want to work on]"
disable-model-invocation: true
---

# goal-helper

*Provided as part of your King Intelligence engagement. Not for resale or redistribution.*

Read the full workflow at `${CLAUDE_PLUGIN_ROOT}/skills/goal-helper/SKILL.md` and execute it exactly, treating `$ARGUMENTS` as what you want to work on. Any sub-files it references live under `${CLAUDE_PLUGIN_ROOT}/skills/goal-helper/`.

Input: $ARGUMENTS
