# Clients

Each client gets a folder in here. We start adding them in Level 2.

Inside each client folder, you'll have a `CLAUDE.md` (about that client — who they are, what they care about, how they talk), notes, and any artifacts (proposals, contracts, recordings).

That way when you're working on a specific client, Claude only sees that client's context. Not your whole life.
