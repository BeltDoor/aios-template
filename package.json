{
  "name": "aios-template",
  "version": "1.0.0",
  "private": true,
  "description": "AI Operating System — cloneable template. The connection kit installs Playwright here; everything else is markdown.",
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
