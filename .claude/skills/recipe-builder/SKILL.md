---
name: recipe-builder
description: STUB. Built in a later slice. Walk the user through turning a real recurring task into a saved recipe (workflow they can run again).
---

# /recipe-builder — STUB

This skill is built in a later slice. For now it's a placeholder.

When built, it will: ask the user which task to automate, walk through the steps with them, build the workflow, save it to `recipes/`, and run it on a real example.
